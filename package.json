{
  "name": "knoxville-tree-services-directory",
  "version": "1.0.0",
  "description": "Independent, source-checked Knoxville tree-service directory",
  "private": true,
  "license": "UNLICENSED",
  "scripts": {
    "test": "node scripts/validate-site.js && node --check js/app.js && node --check js/data.js",
    "start": "python3 -m http.server 4173 --directory .",
    "build": "node scripts/build.js"
  }
}
